
#include <vector>
#include <iostream>
#include <string>
#include <sstream>
#include <fstream>
#include <boost/numeric/ublas/matrix.hpp>
#include <boost/numeric/ublas/io.hpp>

//This is a header file to define typedef datatypes and functions that will be used by all programs in the mandelbrot directory

// typedef to define datatypes
typedef unsigned long int INDEX;
typedef long double VALUE;
typedef std::complex<long double> MAT_VALUE;

// definition of functions that are perfomed on matrix
MAT_VALUE mult2( MAT_VALUE a,  MAT_VALUE b);
MAT_VALUE square(MAT_VALUE a);
MAT_VALUE add2( MAT_VALUE a,  MAT_VALUE b);
MAT_VALUE juliamap( MAT_VALUE a,  MAT_VALUE b);
